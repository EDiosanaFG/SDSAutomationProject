# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install
        working-directory: ./SDS_Extraction_Tool_FrontEnd
      - run: npm run build
        working-directory: ./SDS_Extraction_Tool_FrontEnd
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ISURA_SDS_AUTOMATION }}
          channelId: live
          projectId: isura-sds-automation
          entryPoint: ./SDS_Extraction_Tool_FrontEnd
